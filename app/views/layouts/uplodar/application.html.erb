<!DOCTYPE html>
<html>
<head>
<title>Uplodar</title>
<!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
<%= stylesheet_link_tag    "uplodar/application", :media => "all" %>
<%= javascript_include_tag "uplodar/application" %>
<%= csrf_meta_tags %>
<%= yield(:head) -%>
<meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
<%= render 'uplodar/shared/navbar' %>

<div class="container">
  <div class="row">
    <div class="span12">
      <% flash.each do |name, msg| %>
        <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
        <a class="close" data-dismiss="alert">×</a>
        <%= msg %>
      </div>
    <% end %>
      <div id="main-content">
        <%= yield %>
      </div>
    </div>
  </div>
</div>
</body>
</html>

