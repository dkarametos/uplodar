<%= form_for(@share_assignment) do |f| %>
  <% if @share_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@share_assignment.errors.count, "error") %> prohibited this share_assignment from being saved:</h2>

      <ul>
      <% @share_assignment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
